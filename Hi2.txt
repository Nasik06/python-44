.
:
"
(
)
_
print
if
else
/



ceil - целый
floor - доробный
trunc - обрезаный

print
int - целый
 math
type
input
value

как запустить свой файл
¬место треугольника была стрелочка
s1+s2




input() Ц команда получени€ данных из командной строки

print() Ц вывод сообщени€ в консоль

type() Ц определение типа переменной (и не только)
int -   ÷елые числа (int)
float -  ƒробные числа (float)
complex -    омплексные числа (complex)
str -  —троки (str)
'''' '''' - многострочные строки
/    - знак делени€, выдает дробное (5.0)
// - целое число выдает при делении

'''  ''' - текст между ними будет с разных строк
\n - если добавить к строке, то будет пробел между строками, 
	и переведет на след строчку

x = 'abcde'
len(x)  --- считает сколько символов 
x[::-1]*3 -----переворачивает и на три раза повтора€ет
x[1:-1] ----- отсеткает 1 и последний символ
x[0]--- вз€ть первый символ из строки
len()  - длина строки
УabdeФ * n  - дублирование строки
 import math
math.ceil() Ч округление чисел в большую сторону
math.floor() Ч округление чисел в меньшую сторону
math.trunc() Ч отбрасывание дробной части
math.sqrt(x) - корень квадратный от х
%  - остаток от делени€ 
x = int(input())  - вызывает строчку куда можно вводить и замыкает строчку int-ом
N = int(input('Enter number:\n'))  - с клавиату ввести цифры - после слов 

str.replace(old, new[, max]) - параметры^
---old Ч это стара€ подстрока, подлежаща€ замене
----new Ч это нова€ подстрока, котора€ заменит старую подстроку.
----max Ч если задан этот необ€зательный аргумент max, замен€ютс€ только первые числа вхождений

print(x)
print(type(x))





«адача

¬ывести на экран решение выражени€:

X  = 555 // 3 * (2 + 345) ** 7 Ц 345 + N, где N вводитс€ с клавиатуры


–ешение:
N = int(input('Enter number:\n'))
 
x = 1234 + (123 * 5421) ** 123 + N
 
print(x)
print(type(x))



не работает
x = 'abcde'
len(x)
x[::-1]

